post:
  summary: Stop Walk Test
  description: |
    Stops the walk test process on the panel.
  tags:
    - Installer:8.0:WalkTest
  parameters:
    - $ref: '../../../../openapi.yaml#/components/parameters/UserTokenHeader'
    - $ref: '../../../../openapi.yaml#/components/parameters/SessionTokenHeader'
  requestBody:
    required: false
    content:
      application/json:
        schema:
          type: object
          description: Empty object
        example: {}
  responses:
    '200':
      description: Walk test stopped
      content:
        application/json:
          schema:
            type: array
            items: {}
          example: []
    '401':
      description: Unauthorized — missing or invalid User-Token or Session-Token
